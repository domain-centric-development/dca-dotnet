<Project Sdk="Microsoft.NET.Sdk">
  <!-- A deliberately wrong consumer assembly: one class placed in the reserved building-blocks port namespace.
       Kept apart from the test assembly so the whole-catalog fixtures stay clean while DCA-LAY-005 has a real negative. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <IsPackable>false</IsPackable>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
  </PropertyGroup>
</Project>
